import { styled } from '@mui/material/styles';
import { Box } from '@mui/system';

export const GetReadyBox = styled(Box)({
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
  padding: '30px 0',
  gap: '20px',
})
